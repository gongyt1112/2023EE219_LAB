Run `chmod +x ./gen.sh` to add running permissions.

Use `./gen.sh demo` to compile the demo.c file.

For more reference cases, refer to `https://github.com/NJU-ProjectN/am-kernels/tree/master/tests/cpu-tests/tests`.
